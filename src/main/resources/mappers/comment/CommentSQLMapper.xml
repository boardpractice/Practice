<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.comment.CommentSQLMapper">

    <!-- 댓글 목록 -->
    <select id="getCommentList" resultType="CommentVo">
        select *
        from eden_board_comment
        where board_no = #{board_no}
    </select>

    <!-- 댓글 총 갯수 -->
    <select id="getTotalCommentCount" resultType="int">
        select count(*) from eden_board_comment where board_no = #{board_no}
    </select>

    <!-- 댓글 작성 -->
    <insert id="writeComment">
        insert into eden_board_comment (comment_no, board_no, user_no, comment_content) values (eden_board_comment_seq.nextval, #{board_no}, #{user_no}, #{comment_content})
    </insert>
</mapper>